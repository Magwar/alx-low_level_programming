#include <stdlib.h>
#include <time.h>
/**
* Description: main -Assign a random number to a variable each time it is executed.
*
* Return: 0 if succesful
*/
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
scanf
printf(last digit of %d
	/* your code goes there */
	return (0);
}
